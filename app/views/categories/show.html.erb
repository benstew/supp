<p id="notice"><%= notice %></p>

<div class="container">
  <div class="container">
    <h1><%= @category.name %></h1>
    <p>
      <strong>Description:</strong>
      <%= @category.description %>
    </p>
  </div>

  <div class="container">
    <%= link_to 'Edit', edit_category_path(@category) %> |
    <%= link_to 'Back', categories_path %>
  </div>
</div>

  <br>

<div class="container">
  <div class="container">
    <h3>Relevant Goals</h3>
    <p>Some specific goals relevant to your high level goal</p>
    <table class="table table-striped">
      <thead>
        <tr>
          <th></th>
          <th>Goal</th>
          <th>Description</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <ol>
          <% @goals.each do |goal| %>
            <tr>
              <td><li></li></a></td>
              <td><a href="<%= category_goal_path(@category, goal )%>"><%= goal.name =%></a></td>
              <td><a href="<%= category_goal_path(@category, goal )%>"><%= goal.description %></a></td>
            </tr>
          <% end %>
        </ol>
      </tbody>
    </table>
    |<%= link_to 'New Goal', new_category_goal_path(@category) %>|
  </div>
</div>
